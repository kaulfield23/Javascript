document.addEventListener('DOMContentLoaded', () => {
    const buttons = [];
    buttons.length = 10;
    console.log(buttons);
    buttons.forEach(() => {
        const button = document.createElement('button');
    }) 
        
    
})